import { analysis, analysisXY } from "analysis";
import { Divisor } from "divisor";
import { arithmetics, geometric } from "numberSequence";
import { combination, expected, permutation, repeatTry } from "probability";

module.exports = {
  analysis,
  analysisXY,
  Divisor,
  arithmetics,
  geometric,
  permutation,
  combination,
  repeatTry,
  expected,
};
